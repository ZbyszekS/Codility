public class FrogImp {
    public int solution(int X, int Y, int D) {
        int distance = Y-X;
        int jumps = distance/D;
        if( (D * jumps) < distance ){
            jumps++;
        }
        return jumps;
    }    
}
